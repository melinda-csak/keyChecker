def ReadFile(filePath, separator, header):
    # TODO: read file into a format that is easily useable in the future (for example dataFrame?)
    # use the parameters above!

    df = "TODO"

    return df